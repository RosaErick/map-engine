/** Um lugar só para o endereço do repositório: ele aparece no topo, no sobre e
 *  no rodapé, e três cópias divergem no dia em que o repo mudar de nome.
 *
 *  O produto virou ProjMap, o repositório não: continua `RosaErick/map-engine`.
 *  O nome aqui é o endereço que existe no GitHub, não a marca — trocar por
 *  `projmap` só quebra os três links. Muda quando o repo for renomeado de fato. */
export const REPO_URL = 'https://github.com/RosaErick/map-engine';
